import { useEffect, useState } from "react";
import { LogIn, UserPlus, X } from "lucide-react";
import { useAuth, type RegisterInput } from "../context/AuthContext";

type AuthModalProps = { isOpen: boolean; onClose: () => void; initialMode?: "login" | "register" };
type RegisterDraft = Omit<RegisterInput, "id"> & { confirmPassword: string };
const emptyRegister: RegisterDraft = { firstName: "", lastName: "", username: "", email: "", phone: "", password: "", confirmPassword: "" };
function Field({ label, type = "text", value, onChange, autoComplete }: { label: string; type?: string; value: string; onChange: (value: string) => void; autoComplete?: string }) { return <label className="block"><span className="mb-1.5 block text-xs font-medium text-slate-400">{label}</span><input required type={type} value={value} autoComplete={autoComplete} onChange={(event) => onChange(event.target.value)} className="w-full rounded-xl border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm text-slate-100 outline-none transition placeholder:text-slate-600 focus:border-cyan-400" /></label>; }

export function AuthModal({ isOpen, onClose, initialMode = "login" }: AuthModalProps) {
  const { login, register } = useAuth(); const [mode, setMode] = useState(initialMode); const [identifier, setIdentifier] = useState(""); const [password, setPassword] = useState(""); const [draft, setDraft] = useState<RegisterDraft>(emptyRegister); const [error, setError] = useState("");
  useEffect(() => { if (!isOpen) return undefined; const onKeyDown = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; document.addEventListener("keydown", onKeyDown); return () => document.removeEventListener("keydown", onKeyDown); }, [isOpen, onClose]);
  if (!isOpen) return null;
  const setDraftField = (key: keyof RegisterDraft, value: string) => setDraft((current) => ({ ...current, [key]: value }));
  const submit = (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); if (mode === "login") { const result = login({ identifier, password }); if (result.success) onClose(); else setError(result.error ?? "Đăng nhập thất bại."); return; } if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(draft.email)) { setError("Vui lòng nhập email hợp lệ."); return; } if (draft.password.length < 6) { setError("Mật khẩu phải có ít nhất 6 ký tự."); return; } if (draft.password !== draft.confirmPassword) { setError("Mật khẩu xác nhận không khớp."); return; } const result = register(draft); if (result.success) onClose(); else { setMode("login"); setIdentifier(draft.email); setPassword(""); setError(result.error ?? "Đăng ký thất bại."); } };
  const switchMode = (nextMode: "login" | "register") => { setMode(nextMode); setError(""); };
  return <div className="fixed inset-0 z-[110] flex items-center justify-center p-4" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className="absolute inset-0 bg-black/75 backdrop-blur-sm" /><div role="dialog" aria-modal="true" aria-labelledby="auth-title" className="relative max-h-[calc(100vh-32px)] w-full max-w-lg overflow-y-auto rounded-2xl border border-slate-700 bg-[#121721] p-5 shadow-2xl shadow-black/70 sm:p-6"><div className="mb-5 flex items-start justify-between"><div><div className="mb-2 flex h-10 w-10 items-center justify-center rounded-xl bg-cyan-400/10 text-cyan-300">{mode === "login" ? <LogIn className="h-5 w-5" /> : <UserPlus className="h-5 w-5" />}</div><h2 id="auth-title" className="text-lg font-semibold text-white">{mode === "login" ? "Đăng nhập TradeAI PRO" : "Tạo tài khoản TradeAI PRO"}</h2><p className="mt-1 text-xs text-slate-500">Lưu phiên đăng nhập trên thiết bị này.</p></div><button type="button" aria-label="Đóng" onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-slate-800 hover:text-white"><X className="h-5 w-5" /></button></div><div className="mb-5 grid grid-cols-2 rounded-xl border border-slate-800 bg-slate-950/60 p-1"><button type="button" onClick={() => switchMode("login")} className={`rounded-lg py-2 text-sm font-semibold ${mode === "login" ? "bg-cyan-400/15 text-cyan-200" : "text-slate-500"}`}>Đăng nhập</button><button type="button" onClick={() => switchMode("register")} className={`rounded-lg py-2 text-sm font-semibold ${mode === "register" ? "bg-cyan-400/15 text-cyan-200" : "text-slate-500"}`}>Đăng ký</button></div>{error ? <p role="alert" className="mb-4 rounded-lg border border-rose-500/20 bg-rose-500/10 px-3 py-2 text-sm text-rose-300">{error}</p> : null}<form onSubmit={submit} className="space-y-4">{mode === "login" ? <><Field label="Username hoặc Email" value={identifier} onChange={setIdentifier} autoComplete="username" /><Field label="Mật khẩu" type="password" value={password} onChange={setPassword} autoComplete="current-password" /></> : <><div className="grid gap-4 sm:grid-cols-2"><Field label="Họ" value={draft.firstName} onChange={(value) => setDraftField("firstName", value)} autoComplete="given-name" /><Field label="Tên" value={draft.lastName} onChange={(value) => setDraftField("lastName", value)} autoComplete="family-name" /></div><Field label="Username" value={draft.username} onChange={(value) => setDraftField("username", value)} autoComplete="username" /><Field label="Email" type="email" value={draft.email} onChange={(value) => setDraftField("email", value)} autoComplete="email" /><Field label="Số điện thoại" type="tel" value={draft.phone} onChange={(value) => setDraftField("phone", value)} autoComplete="tel" /><div className="grid gap-4 sm:grid-cols-2"><Field label="Mật khẩu" type="password" value={draft.password} onChange={(value) => setDraftField("password", value)} autoComplete="new-password" /><Field label="Xác nhận mật khẩu" type="password" value={draft.confirmPassword} onChange={(value) => setDraftField("confirmPassword", value)} autoComplete="new-password" /></div></>}<button type="submit" className="w-full rounded-xl bg-cyan-400 px-4 py-3 text-sm font-bold text-slate-950 transition hover:bg-cyan-300">{mode === "login" ? "Đăng nhập" : "Tạo tài khoản"}</button></form></div></div>;
}